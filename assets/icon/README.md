Place a 1024x1024 PNG file named app_icon.png here.

Design: Book icon in white/accent color (#E94560) on #0f0f1a dark background.
Simple, minimal, elegant.

Also place app_icon_fg.png for the Android adaptive icon foreground layer.
This should be the book icon only (no background), centered on a transparent canvas.

To generate the launcher icons after placing files here, run:
  dart run flutter_launcher_icons
